/.phpunit.cache
/node_modules
# /public/build — tracked in git for consistent deployments
/public/hot
/public/storage
# AI-generated social images — never commit, regenerated on demand
/public/social
/storage/*.key
/storage/pail
/vendor
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed
.env.coolify
.env.local
docker-compose.override.yml
.claude/
.env.bak.*
.mailcow-credentials
# Chatbot QA reports — generated locally, served from public/, not in git
/public/malinco_*.html
# Enterprise architecture PDF — served behind obscured URL, not for GitHub
/public/downloads/sambla-enterprise-*.pdf
.openai-admin-credentials
.env.bak*
# Social-image prompt-iteration artifacts — generated during dev, not for git
/public/test-gpt-image-2/
# Side-projects served from public/ but not part of voicebot-saas
/public/ancpi-v2
/public/palma
# Live status page — runtime artifact while Claude lucrează la redesign
/public/sambla-status.html
/tmp/sambla-status-state.json
